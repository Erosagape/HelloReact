****React

Setting Required:
  node.js v8
  jdk 1.8
  
npm setup:
  npm install -g create-react-app

create new project:
  create-react-app <ชื่อโปรเจค>
  
running server:
  cd <ชื่อโปรเจค>
  npm start
  
